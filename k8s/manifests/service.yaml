apiVersion: v1
kind: Service
metadata:
  name: go-web-app
spec:
  selector:
    app.kubernetes.io/name: go-web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

    selector:
      app: go-web-app  
    type: ClusterIp